## 1. 持久化实现

- [x] 1.1 修改 `ClusterConfigPersister.save`，规范化目标路径并在写入前创建父目录
- [x] 1.2 使用同目录临时文件完成写入、原子替换/普通替换降级，并在失败时清理临时文件后抛出带上下文的 IOException
- [x] 1.3 增强 `NettyRedisServer.saveClusterConfig` 的失败日志，确保成功日志只在保存完成后输出

## 2. 回归测试

- [x] 2.1 增加首次保存生成非空 `nodes.conf` 且自动创建父目录的测试
- [x] 2.2 增加重复保存覆盖旧文件并清理临时文件的测试
- [x] 2.3 增加保存失败场景的异常与临时文件清理验证

## 3. 验证

- [x] 3.1 运行集群模块相关单元测试
- [x] 3.2 运行项目构建并确认无回归
