## 1. 复制数据路径测试

- [x] 1.1 添加失败测试，证明 master 成功写入会进入 backlog 并由 slave 应用到共享 `MemoryStore`
- [x] 1.2 添加复制流拆包、粘包和事务重放测试

## 2. 端到端复制链路

- [x] 2.1 在服务启动时完整装配主从复制管理器、命令处理器、存储及持久化依赖
- [x] 2.2 在成功写命令路径传播原始 RESP 帧，并排除只读、失败、重定向和复制来源命令
- [x] 2.3 实现 slave 对传播 RESP 流的增量解析和命令执行

## 3. 集群角色生命周期

- [x] 3.1 定义集群模块可依赖的复制生命周期回调，并在服务层提供实现
- [x] 3.2 让 `CLUSTER REPLICATE` 启动或切换到目标 master 的复制连接
- [x] 3.3 让提升节点停止上游复制，并让降级节点跟随新 master，保证重复通知幂等

## 4. 故障转移回归验证

- [x] 4.1 添加集成测试：确认候选 slave 已同步数据后触发 master 故障，验证新 master 保留故障前数据
- [x] 4.2 验证故障转移后的新增写入能够同步到恢复并降级的原 master
- [x] 4.3 运行 replication、server、cluster 模块测试和完整 Maven 构建
